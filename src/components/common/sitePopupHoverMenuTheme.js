export const theme = {
  '$extend': 'menu',
  boundary: document.body, // keeps within scrollbar-gutter
  triggers: ['hover', 'focus'],
  autoHide: true,
  placement: 'bottom',
  delay: { show: 400, hide: 0 }
}
